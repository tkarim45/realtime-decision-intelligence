"""Real-Time Multi-Modal Decision Intelligence. AIOps domain (see docs/04-domain.md).

Modules: broker (durable log), features (online windowed), model (classifier), anomaly
(unsupervised detector), conformal (prediction sets), scoring (the assembled hot path).
"""

__version__ = "0.1.0"
